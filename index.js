import express from "express"; import cors from "cors"; import OpenAI from "openai"; const app = express(); app.use(cors()); app.use(express.json()); const client = new OpenAI({ apiKey: process.env.OPENAI_KEY }); app.post("/ask", async (req, res) => { try { const question = req.body.message; const aiResponse = await client.chat.completions.create({ model: "gpt-4o-mini", messages: [{ role: "user", content: question }] }); const reply = aiResponse.choices[0].message.content; res.json({ reply }); } catch (err) { console.error(err); res.json({ reply: "Sorry, the AI is not available right now." }); } }); app.listen(3000, () => console.log("Server running on port 3000"));
